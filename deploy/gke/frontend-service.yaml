apiVersion: v1
kind: Service
metadata:
  name: vibetrader-frontend
  namespace: vibetrader
spec:
  type: LoadBalancer
  selector:
    app: vibetrader-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080

